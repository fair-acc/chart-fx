<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>de.gsi</groupId>
        <artifactId>chartfx</artifactId>
        <version>${revision}${sha1}${changelist}</version>
        <relativePath>..</relativePath>
    </parent>
    <groupId>de.gsi.math</groupId>
    <artifactId>chartfx-math</artifactId>
    <name>math</name>

    <description>
        A small set of math routines that can operate directly on the DataSet primitive for fitting,
        computing spectra, linear algebra, FIR/IIR filtering, and other functionalities common to signal processing.
    </description>

    <dependencies>
        <dependency>
            <groupId>de.gsi.generate</groupId>
            <artifactId>chartfx-generate</artifactId>
            <version>${project.version}</version>
            <scope>compile</scope> <!-- we need it to be compiled first, but not be part of the project -->
        </dependency>
        <dependency>
            <groupId>de.gsi.dataset</groupId>
            <artifactId>chartfx-dataset</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.github.wendykierp</groupId>
            <artifactId>JTransforms</artifactId>
            <version>3.1</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-math3</artifactId>
            <version>3.6.1</version>
        </dependency>
        <dependency>
            <groupId>net.jafama</groupId>
            <artifactId>jafama</artifactId>
            <version>2.3.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>codegen</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>java</goal>
                        </goals>
                        <configuration>
                            <mainClass>de.gsi.generate.CodeGenerator</mainClass>
                            <skip>false</skip>
                            <arguments>
                                <argument>${project.build.sourceDirectory}</argument> <!-- Input Sources -->
                                <argument>${project.build.directory}/generated-sources/annotations</argument> <!-- Output Sources -->
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
